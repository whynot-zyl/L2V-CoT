torch>=2.0.0
transformers>=4.36.0
accelerate>=0.24.0
pillow>=9.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
tqdm>=4.65.0
einops>=0.7.0

# Optional: for quantization support
# bitsandbytes>=0.41.0

# Optional: for VLMEvalKit evaluation
# Install separately:
# git clone https://github.com/open-compass/VLMEvalKit.git
# cd VLMEvalKit && pip install -e .

# Development / testing
pytest>=7.0.0
